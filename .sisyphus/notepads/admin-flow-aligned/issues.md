# Issues

- 2026-04-11: `lsp_diagnostics` could not run for the changed TypeScript files because `typescript-language-server` is configured but not installed in this environment. Verification fell back to passing Vitest suites plus `npm run typecheck`.
- 2026-04-11: The same LSP limitation applied after the admin route change, so the final verification path remained the targeted Vitest file plus `rtk npm run typecheck`.
- 2026-04-11: The export test initially failed with the scaffold placeholder because `GET /admin/:secret/api/export.csv` still returned `notImplemented`; the fix was to replace the branch with a real CSV response and keep the method/secret checks unchanged.

- `lsp_diagnostics` could not complete in this environment because the configured `typescript-language-server` and `biome` binaries are not installed.

- 2026-04-11: `lsp_diagnostics` still cannot run on `src/worker/db/admin-records.ts` and `test/unit/admin-records.test.ts` because `typescript-language-server` is configured but missing in this environment, so verification relied on passing Vitest suites plus `rtk npm run typecheck`.
- 2026-04-11: `lsp_diagnostics` remained unavailable for `src/worker/routes/admin.ts`, `src/worker/db/admin-records.ts`, and `test/integration/admin-api.test.ts` because `typescript-language-server` is still not installed in this environment; verification again fell back to the targeted Vitest suite plus `rtk npm run typecheck`.
- 2026-04-11: `lsp_diagnostics` is still blocked for the admin delete change because `typescript-language-server` is not installed here; the successful verification path was targeted Vitest plus `rtk npm run typecheck`.
- 2026-04-13: `lsp_diagnostics` is still blocked for `public/admin/app.js`, `public/admin/index.html`, and `test/integration/admin-page-app.test.ts` because `typescript-language-server` and `biome` are not installed in this environment; verification therefore relied on `rtk npm test -- test/integration/admin-page-app.test.ts`, `rtk npm test`, and `rtk npm run typecheck`.
- 2026-04-13: No playwright.config.ts, Playwright dependencies, or 	est/e2e/ directory exist yet; Task 9 must add all three rather than extending an existing browser harness.\n- 2026-04-13: 	sconfig.json does not include root config files outside itest.config.ts, so a root-level Playwright config may be invisible to 	sc unless the typecheck strategy is adjusted or the file stays type-safe without compilation coverage.\n- 2026-04-13: The admin route depends on ADMIN_SECRET and EVENT_DATE being present in the Playwright-managed environment; missing either will fail the page/API before the browser spec can reach the records table.\n- 2026-04-13: The current repo already has deterministic admin temp logs under .sisyphus/, but no established E2E seed/setup artifact naming yet; a new helper/seed file will need a convention that does not collide with those transient wrangler logs.\n

- 2026-04-13: No playwright.config.ts, Playwright dependencies, or test/e2e/ directory exist yet; Task 9 must add all three rather than extending an existing browser harness.
- 2026-04-13: tsconfig.json does not include root config files outside vitest.config.ts, so a root-level Playwright config may be invisible to tsc unless the typecheck strategy is adjusted or the file stays type-safe without compilation coverage.
- 2026-04-13: The admin route depends on ADMIN_SECRET and EVENT_DATE being present in the Playwright-managed environment; missing either will fail the page/API before the browser spec can reach the records table.
- 2026-04-13: The current repo already has deterministic admin temp logs under .sisyphus/, but no established E2E seed/setup artifact naming yet; a new helper/seed file will need a convention that does not collide with those transient wrangler logs.

- No Playwright config, e2e directory, or browser-test scripts exist yet; Task 9 must introduce them rather than extend current files.
- No explicit Wrangler dev port/binding overrides are present in repo config, so any E2E setup that depends on a fixed origin will need to decide whether to keep Wrangler defaults or add explicit port flags in scripts.
- Seed data currently targets the earlier student/mentor demo (5+5 people) and not an admin-specific deterministic record set; Task 9 will need a separate bootstrap layer if admin E2E requires fixed scan records, not just people rows.
- 2026-04-13: Initial Playwright red runs failed in two distinct ways: browsers were not installed yet (`npx playwright install chromium` fixed that) and `wrangler dev` ignored Worker env passed only through process env, which required switching to explicit `--var ADMIN_SECRET:... --var EVENT_DATE:...` flags.
- 2026-04-13: `lsp_diagnostics` remained unavailable for the new Playwright files because `typescript-language-server` is still not installed in this environment; final verification relied on the passing `rtk npx playwright test test/e2e/admin-flow.spec.ts` and `rtk npm run typecheck` commands instead.
